{
  "private": true,
  "workspaces": [
    "ajt",
    "www"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces --if-present",
    "watch": "npm run watch --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "examples:dev": "docker compose -f www/docker-compose.yaml up"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^25.3.3",
    "eslint": "^10.3.0",
    "globals": "^17.4.0"
  }
}
