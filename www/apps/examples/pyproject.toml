[project]
name = "examples"
version = "0.1.0"
description = "Examples app"
readme = "README.md"
authors = [
    { name = "Lucky Salmon", email = "lucky-salmon@tutanota.com" }
]
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.3",
    "markdown>=3.10.2",
    "pillow>=12.2.0",
]

[build-system]
requires = ["uv_build>=0.11.4,<0.12.0"]
build-backend = "uv_build"
