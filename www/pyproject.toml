[project]
name = "site"
version = "0.1.0"
description = "ajt-site"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.3",
    "markdown>=3.10.2",
    "pillow>=12.2.0",
]
