[project]
name = "site"
version = "0.1.0"
description = "ajt-site"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.3",
    "examples",
    "whitenoise>=6.12.0",
]

[tool.uv.workspace]
members = [
    "apps/*",
]

[tool.uv.sources]
examples = { workspace = true }
